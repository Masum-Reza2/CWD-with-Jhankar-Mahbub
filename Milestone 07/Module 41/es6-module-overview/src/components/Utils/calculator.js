const add = (first, second) => {
    return first + second;
}

// single functio
// export default add;

const multiply = (a, b) => {
    return a * b
}

const devide = (a, b) => {
    return a / b;
}

// bigname
const PrintHelloWorldOnTheConsole = () => {
    console.log('hello world')
}

// multiple functionf
export { add, multiply, devide, PrintHelloWorldOnTheConsole }